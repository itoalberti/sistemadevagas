// DADOS DO BACKEND:
export const port = 4000;
export const hostname = 'localhost';

export const urlCandidato = `http://${hostname}:${port}/candidato`;
export const urlVaga = `http://${hostname}:${port}/vaga`;
export const urlCandidatura = `http://${hostname}:${port}/candidatura`;
