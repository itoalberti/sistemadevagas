// DADOS DO BACKEND:
export const port = 4000;
export const hostname = 'localhost';

export const urlCandidato = `http://${hostname}:${port}/candidatos`;
export const urlVaga = `http://${hostname}:${port}/vagas`;
export const urlInscricoes = `http://${hostname}:${port}/inscricoes`;
